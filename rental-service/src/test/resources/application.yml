rental:
  events:
    publisher: logging

spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: true
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration
